<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".staff.AddEvent">

    <TextView
        android:id="@+id/title"
        android:text="@string/create_news"
        android:textSize="40sp"
        android:gravity="center"
        android:textStyle="bold"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="50dp"
        />

    <EditText
        android:id="@+id/add_news_title"
        android:layout_marginTop="50dp"
        android:layout_below="@id/title"
        android:hint="@string/news_title_hint"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        />

    <EditText
        android:id="@+id/news_description"
        android:layout_below="@id/add_news_title"
        android:hint="@string/description"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        />

    <EditText
        android:id="@+id/add_image_url"
        android:layout_below="@id/news_description"
        android:hint="@string/image_url_hint"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        />

    <Button
        android:id="@+id/add_news_button"
        android:text="@string/create_news"
        android:layout_width="300dp"
        android:layout_height="80dp"
        android:layout_below="@id/add_image_url"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@color/StudioXPink"
        />

</RelativeLayout>